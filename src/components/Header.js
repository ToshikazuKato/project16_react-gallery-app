import React from 'react';

const Header = () => {
        return(
            <div className="header">
                <h1>React Gallery with API</h1>
            </div>
        );
}

export default Header;